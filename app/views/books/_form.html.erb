<link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" rel="stylesheet"
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(model: book, local: true) do |form| %>
      <% if book.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

          <ul>
          <% book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= form.label :availability, class: "btn btn-info" %>
        <%= form.check_box :availability, class: "form-group"   %>
      </div><br>

      <div class="field">
        <%= form.label :author, class: "btn btn-info" %>
        <%= form.text_field :author, class: "form-control" %>
      </div><br>

      <div class="field">
        <%= form.label :genre, class: "btn btn-info center-block" %>
        <%= form.text_field :genre, class: "form-control center-block"   %>
      </div><br>

      <div class="field">
        <%= form.label :title, class: "btn btn-info" %>
        <%= form.text_field :title, class: "form-control"  %>
      </div><br>

      <div class="field">
        <%= form.label :copies_available, class: "btn btn-info" %>
        <%= form.number_field :copies_available, class: "form-control"  %>
      </div><br>

      <div class="field">
        <%= form.label :user %>
        <%= form.collection_select :user_id, User.all, :id, :full_name, include_blank: true %>
      </div><br>


      <div class="actions">
        <%= form.submit "Submit", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>
